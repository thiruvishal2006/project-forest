# Runtime database directory

The application creates its SQLite database in this directory on startup by default. Database files are ignored by Git and must not be published as source data. Configure a different path with `DATABASE_PATH` when needed.
